# Results

`results/paper/` contains only minimal CSV files needed to verify paper table
and figure values. Raw experiment dumps, model checkpoints, raw LLM logs,
calibration states, and episodic memory states are intentionally excluded.
